import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    textAlign: "center",
    fontSize: 16,
    lineHeight: 19,
    color: "#008CBA",
  },
});

export default styles;
